---
title: "cpp错题本"
date: "2025-05-23"
tags:
---
## static

 #### 类的 static 成员
 - c++11以前, 非const的静态数据成员要在类外定义和初始化, 而 const 在类内
 - 根据 c++ primer p270，可以为静态成员提供const整数类型的类内初始值

## class, struct

#### struct 的定义
```cpp
struct color  // color 结构名 optional
{    int red;
    int green;
    int blue;
} c1; // c1 变量名 optional

typedef struct
{    int red;
     int green;
     int blue;
}COLOR;
COLOR c1;
```

## 函数

#### 形参和实参
- **数组**作为函数参数传递时，形参数组必须指明**除第一维以外所有维的长度**。二维数组不能以指针数组传递
- 因为参数的参数只能接收一个地址, 不知道其他维长度就无法计算偏移(第一维可以通过类型得到)

## 数组

#### new 运算符创建数组
在C++中，使用new运算符分配内存时有两种常见形式:  
1. `new int[10]` - 只分配内存空间,**不进行初始化**  
2. `new int[10]()` - 分配内存空间并**进行零初始化**, 调用了构造函数

#### 初始化
大括号方式初始化, 未设值的部分为0
```cpp
static int a[3][4] = {{3, 4},{2, 8, 6}};
```

#### 数组的创建方式
```cpp
char szData[3] = “ab”; // 注意\0
char szData[] = “abc”;
char szData[] = {“abc”};  // 其实是{'a', 'b', 'c', '\0'} 的特殊写法
```
## 平台编译, 占用空间, 存储位置


#### 类型长度
32位编译器：32位系统下指针占用4字节`
- char ：1个字节
- char*（即指针变量）: 4个字节（32位的寻址空间是2^32, 即32个bit，也就是4个字节。同理64位编译器）
- short int : 2个字节
- int：  4个字节
- unsigned int : 4个字节
- float:  4个字节
- double:   8个字节
- long:   4个字节
- long long:  8个字节
- unsigned long:  4个字节

64位编译器：64位系统下指针占用8字节
- char ：1个字节
- char*(即指针变量): 8个字节
- short int : 2个字节
- int：  4个字节
- unsigned int : 4个字节
- float:  4个字节
- double:   8个字节
- long:   8个字节
- long long:  8个字节
- unsigned long:  8个字节

#### 影响类的大小的因素
这些会影响：  
- 成员个数：每个成员变量都会占用内存空间，成员越多，类的大小就越大。  
- 是否有虚函数：如果类中含有虚函数，编译器会为该类生成虚函数表指针(vptr)，这个指针会占用额外的内存空间。  
- 对齐方式：内存对齐是编译器为了提高程序性能而采取的一种策略，不同的对齐方式会导致类的大小不同。比如4字节对齐和8字节对齐会产生不同的类大小。  
这些不影响：  
- 函数个数：普通成员函数不占用类的内存空间，因为它们存储在**代码段**中。  
- 虚函数个数：虚函数的数量并不直接影响类的大小，因为不管有多少个虚函数，类中只需要一个虚函数表指针。  
- 静态变量个数：静态变量属于类而不属于对象，它们存储在**全局数据区**，不会影响类的大小。

#### char 有无符号?
链接：[https://www.nowcoder.com/questionTerminal/fdb4a0a2e1ea4561b0d2c6456dea9126](https://www.nowcoder.com/questionTerminal/fdb4a0a2e1ea4561b0d2c6456dea9126)
arm-linux-gcc 规定 char 为 unsigned char   0~255
vc 编译器、x86上的 gcc 规定 char 为 signed char -128~127

#### 内存存储位置

**堆区变量**
- 通过 new 操作符动态分配的  
  
**栈区变量**  
- 在函数中定义的局部变量
- 在代码块中定义的局部变量
  
**全局/静态区变量**
- 全局变量
- 静态成员变量

## STL

#### 下标操作
支持
1. vector(): 支持随机访问,可以通过下标直接访问元素  
2. deque(): 双端队列,支持随机访问,可以使用下标访问元素  
3. map(): 关联容器,可以通过键值使用下标访问元素  
4. unordered_map(): 哈希表实现的map,同样支持通过键值的下标访问  
5. string(): 字符串类型,支持通过下标访问单个字符  
  
不支持下标运算符`[]`的容器:  
  
6. list(): 双向链表结构,只能通过迭代器顺序访问,不支持随机访问  
7. set(): 集合容器,元素具有唯一性且有序,只能通过迭代器访问  
8. unordered_set(): 哈希表实现的集合,同样只能通过迭代器访问  
9. stack(): 栈结构,只能访问栈顶元素,不支持随机访问

## 类型, 运算, 运算符, 优先级

#### 优先级
类型转换 > 计算
#### 混合运算
数据类型按照精度和范围从小到大的顺序为: char < int < float < double

两种类型混合运算, 会将小精度转成大精度
例如: char, int -> int

#### 位运算
- 位运算只能作用于整型(byte、short、int、long)和字符型(char)数据
- 右移运算时
    - 对于**有符号数**,如果原数为正数则高位补0,如果原数为负数则高位补1。
    - 只有无符号右移(>>>)才是无论何时高位都补0。

#### new 运算符
- new 创建的对象不会自动回收, 需要配合 delete 使用. new 创建的数组则是 `delete[]`
- new 创建对象可以不初始化.
- 创建的对象存储在 堆区

#### 自增自减
- 自增自减操作符只能用于变量, 不能用于表达式
    - ++(a++) 错误, a++是一个表达式的值

## OOP 特性

#### 派生类继承
**可继承**
- 非静态成员函数 
- 私有成员: 是的, 会继承私有成员, 但没有访问权限
  
**不可继承**
- **构造函数**:  构造函数, **析构函数**以及**拷贝构造函数**都不能被继承,每个类都必须定义自己的构造函数。派生类可以通过构造函数初始化列表调用基类的构造函数,但这不是继承关系。  
- **静态成员函数**:  静态成员函数属于整个类而不是某个对象,它不能被继承。派生类可以访问基类的公有静态成员函数,但这是通过作用域解析运算符`(: :)`来实现的,而不是继承。  
- 赋值操作符:  赋值操作函数(**operator=**)不能被继承。每个类都需要定义自己的赋值操作函数,以确保正确处理该类特有的资源管理。

## 函数理解

#### rewind
重新定位到文件读写的首地址(即文件开头)